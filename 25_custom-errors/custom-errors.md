#### Go's custom error types (implementing the `error` interface) map to JavaScript classes that extend `Error`; `errors.As` type-checking is replaced by `instanceof`.
___
##### Run Command:

`$ node custom-errors.js`


##### Results:

`42`
`can't work with it`
