#### Go's `net.Listen`/`net.Conn` TCP server maps to Node.js `net.createServer`; this self-contained example starts a server, connects a client, exchanges a message, and closes cleanly.
___
##### Run Command:

`$ node tcp-server.js`


##### Results:

`ACK: HELLO`
