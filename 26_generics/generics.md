#### Go generics with type parameters map directly to TypeScript generics; the `.js` file uses plain JavaScript with dynamic typing and no generic syntax.
___
##### Run Command:

`$ node generics.js`

`$ npx tsx generics.ts`

##### Results:

`index of zoo: 2`
`list: [ 10, 13, 23 ]`
